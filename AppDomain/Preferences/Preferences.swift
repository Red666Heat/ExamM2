import Foundation

public enum Preferences {
    // swiftlint:disable force_unwrapping
    public enum Server {
        public static var serverBaseUrl = URL(string: "https://api-key.fusionbrain.ai")!
    }
}
